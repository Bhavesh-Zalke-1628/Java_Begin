ate an object of AdvCalc
        AdvCalc obj = new AdvCalc();

        // Use the inherited add method from Calc
        obj.add(10, 20);

        // Use the multi method from AdvCalc
        obj.multi(10, 20);
